# Server host/interface to bind to. Default is empty ("") to bind all interfaces (IPv4 + IPv6).
# Use "127.0.0.1" or "localhost" to restrict access to local machine only.
host: ""

# OAuth client credentials for provider login flows.
# Gemini CLI / Code Assist and Antigravity OAuth use their official OAuth clients by default.
# Set credentials only when you intentionally want to override those clients.
# You can set overrides either in config.yaml or via environment variables:
#   - Gemini:      CLIRELAY_GEMINI_OAUTH_CLIENT_ID / CLIRELAY_GEMINI_OAUTH_CLIENT_SECRET
#   - Antigravity: CLIRELAY_ANTIGRAVITY_OAUTH_CLIENT_ID / CLIRELAY_ANTIGRAVITY_OAUTH_CLIENT_SECRET
oauth-clients:
  gemini:
    client-id: ""
    client-secret: ""
  antigravity:
    client-id: ""
    client-secret: ""

# Server port
port: 8317

# Project timezone (IANA name). Affects "today" boundaries and day-based aggregation in monitoring/usage pages.
# Examples: "Asia/Shanghai", "UTC", "America/Los_Angeles"
timezone: ""

# Redis settings for usage statistics persistence
redis:
  enable: false
  addr: "127.0.0.1:6379"
  password: ""
  db: 0

# TLS settings for HTTPS. When enabled, the server listens with the provided certificate and key.
tls:
  enable: false
  cert: ""
  key: ""

<<<<<<< HEAD
# Explicit browser origins allowed to call API routes cross-origin.
# Leave empty to disable cross-origin browser access by default.
# Chrome extension origins (`chrome-extension://<extension-id>`) are allowed by default.
# Examples:
# cors-allow-origins:
#   - "https://admin.example.com"
#   - "http://localhost:5173"
cors-allow-origins: []

# Reverse proxies whose X-Forwarded-For / X-Real-IP headers may be trusted.
# Leave empty for direct exposure. For Docker bridge + Nginx, trust only the
# proxy/container subnet, for example:
# trusted-proxies:
#   - "172.18.0.0/16"
trusted-proxies: []

# Optional "home" control plane integration over Redis protocol.
home:
  enabled: false
  host: "127.0.0.1"
  port: 6379
  password: ""
  # Keep CPA pinned to the configured home address instead of switching to CLUSTER NODES entries.
  # Useful when Home is behind NAT, Docker networking, or a reverse proxy.
  disable-cluster-discovery: false
  # Optional TLS for the outbound Redis connection to the home control plane.
  # Enable this when connecting through rediss:// or an SSL stream proxy.
  tls:
    enable: false
    # Optional SNI/certificate name override. Leave empty to use the configured home host.
    server-name: ""
    # Trust a private CA bundle in addition to system roots.
    ca-cert: ""
    # Only for testing self-signed endpoints; disables certificate verification.
    insecure-skip-verify: false

=======
>>>>>>> upstream/main
# Management API settings
remote-management:
  # Whether to allow remote (non-localhost) management access.
  # When false, only localhost can access management endpoints (a key is still required).
  allow-remote: false

  # Management key. If a plaintext value is provided here, it will be hashed on startup.
  # All management requests (even from localhost) require this key.
  # Leave empty to disable the Management API entirely (404 for all /v0/management routes).
  secret-key: ""

  # Disable the bundled management control panel asset download and HTTP route when true.
  disable-control-panel: false

  # Disable periodic background sync of the management panel release asset when true.
  # If the local panel asset is missing, the server may still try to download it on first access.
  disable-auto-update-panel: false

  # GitHub repository for the management control panel. Accepts a repository URL or releases API URL.
  panel-github-repository: "https://github.com/zuohuadong/codeProxy"

# Authentication directory (supports ~ for home directory).
# In Docker/cloud deployments, AUTH_PATH overrides this value at runtime.
auth-dir: "~/.cli-proxy-api"

# API keys for authentication
api-keys:
  - "your-api-key-1"
  - "your-api-key-2"
  - "your-api-key-3"

# When true, allow /v1 and /v1beta requests without API keys when no keys/providers
# are configured. Not recommended for production.
allow-unauthenticated: false

# Enable debug logging
debug: false

# Enable pprof HTTP debug server (host:port). Keep it bound to localhost for safety.
pprof:
  enable: false
  addr: "127.0.0.1:8316"
  allow-remote: false

# When true, disable high-overhead HTTP middleware features to reduce per-request memory usage under high concurrency.
commercial-mode: false

# When true, write application logs to rotating files instead of stdout
logging-to-file: false

# Maximum total size (MB) of log files under the logs directory. When exceeded, the oldest log
# files are deleted until within the limit. Set to 0 to disable.
logs-max-total-size-mb: 0

# Maximum number of error log files retained when request logging is disabled.
# When exceeded, the oldest error log files are deleted. Default is 10. Set to 0 to disable cleanup.
error-logs-max-files: 10

# When false, disable in-memory usage statistics aggregation
usage-statistics-enabled: false

<<<<<<< HEAD
# Detailed request/response body retention settings for usage.db.
request-log-storage:
  # When true, persist full request and response bodies in SQLite.
  # When false, stop writing new content while preserving existing stored content.
  store-content: true

  # Keep full request/response bodies for the last N days.
  # Set to 0 to retain full content indefinitely.
  # The lightweight request metadata rows are retained separately.
  content-retention-days: 30

  # Run background content cleanup every N minutes.
  cleanup-interval-minutes: 1440

  # Cap the total size of stored request/response bodies (MB).
  # When exceeded, the oldest full bodies are pruned early even if their retention days are not over.
  # Default is 1024 (≈1GB). Set to 0 to disable the size cap.
  max-total-size-mb: 1024

  # Reclaim disk space after cleanup. This can be I/O intensive on very large databases.
  vacuum-on-cleanup: true

# Optional request context retrieval for large Codex/OpenAI-compatible payloads.
# This trims oversized request history before forwarding while preserving recent turns
# and retrieving older keyword-relevant items.
# context-retrieval:
#   enabled: false
#   models:
#     - name: "*codex*"
#       protocol: "openai-response"
#     - name: "*codex*"
#       protocol: "openai"
#   max-input-bytes: 1600000
#   preserve-recent-turns: 10
#   chunk:
#     max-bytes: 12000
#   retrieval:
#     top-k: 40
#     strategy: "keyword"
#   # Optional second pass: if primary retrieval still exceeds budget, retry with
#   # a smaller budget and truncate oversized retained item text with prefix/suffix.
#   secondary:
#     enabled: true
#     max-input-bytes: 800000
#     preserve-recent-turns: 6
#     top-k: 24
#     max-summary-bytes: 2000
#     max-item-bytes: 24000
#   codex-aware:
#     enabled: true
#     preserve-tool-pairs: true
#     # off, drop-orphans. drop-orphans removes historical function_call items
#     # whose matching function_call_output is not present after context reduction.
#     tool-pair-repair: "drop-orphans"
#     insert-summary: true
#     max-summary-bytes: 4000
#     preserve-recent-commands: 8
#     preserve-recent-errors: 8

# Optional request policies for routing around provider/model limits.
# Policies can skip a channel or reject locally when request size/tools exceed
# configured limits. Use skip-channel to fall back to the next available provider.
# request-policies:
#   - name: "glm-5.1-large-request-guard"
#     match:
#       # No requested-models needed: policy matches by upstream provider/model,
#       # so any client model aliased to GLM-5.1 is automatically protected.
#       upstream-providers: ["bigmodel-coding"]
#       upstream-models: ["glm-5.1"]
#     limits:
#       max-request-bytes: 600000
#     over-limit:
#       action: "skip-channel"
#   - name: "glm-5.1-tool-heavy-skip"
#     match:
#       upstream-providers: ["bigmodel-coding"]
#       upstream-models: ["glm-5.1"]
#     limits:
#       min-tool-calls: 16
#     over-limit:
#       action: "skip-channel"
#   - name: "glm-5.1-tools-skip"
#     match:
#       upstream-providers: ["bigmodel-coding"]
#       upstream-models: ["glm-5.1"]
#       request-features: ["tools"]
#     over-limit:
#       action: "skip-channel"
#   - name: "glm-5.1-long-thread-skip"
#     match:
#       upstream-providers: ["bigmodel-coding"]
#       upstream-models: ["glm-5.1"]
#     limits:
#       min-input-items: 120
#     over-limit:
#       action: "skip-channel"

# Optional provider preferences for model-scoped upstream priority overrides.
# This keeps fallback behavior intact while making a specific provider win first.
# provider-preferences:
#   - name: "prefer-bigmodel-coding-for-glm-5.1"
#     match:
#       upstream-providers: ["bigmodel-coding"]
#       upstream-models: ["glm-5.1"]
#     priority: 100

# Optional media-to-text adapter for text-only upstream models.
# It runs after alias resolution, so the rule below matches the real route:
# requested gpt-5.3-codex -> bigmodel-coding/glm-5.1. Native GPT-5.5/5.4
# channels are unaffected.
# multimodal-adapters:
#   enabled: true
#   default-action: "extract" # extract, reject, strip
#   unavailable-action: "reject" # reject, strip, pass-through
#   inject-as: "visual_context"
#   max-media-items: 4
#   max-output-bytes: 12000
#   rules:
#     - name: "glm-5.1-codex-vision"
#       match:
#         requested-models: ["gpt-5.3-codex"]
#         upstream-providers: ["bigmodel-coding"]
#         upstream-models: ["glm-5.1"]
#         protocols: ["openai-response", "openai"]
#       extractor: "zai-vision-http"
#   extractors:
#     - name: "zai-vision-http"
#       type: "zai-vision-http"
#       endpoint: "https://open.bigmodel.cn/api/paas/v4"
#       headers:
#         Authorization: "Bearer ${Z_AI_API_KEY}"
#       tool-name: "glm-5.1"
#       env:
#         identity_fingerprint: "codex"
#         max_tokens: "512"
#         disable_thinking: "true"
#       timeout-seconds: 60
#     - name: "zai-vision-mcp"
#       type: "mcp"
#       command: "npx"
#       args: ["-y", "@z_ai/mcp-server@latest"]
#       env:
#         Z_AI_API_KEY: "${Z_AI_API_KEY}"
#         Z_AI_MODE: "ZHIPU"
#       tool-name: "image_analysis"
#       timeout-seconds: 60
=======
# How long (in seconds) usage queue items are retained in memory for the Management API.
# The local Redis RESP usage output is disabled.
# Default: 60. Max: 3600.
redis-usage-queue-retention-seconds: 60
>>>>>>> upstream/main

# Proxy URL. Supports socks5/http/https protocols. Example: socks5://user:pass@192.168.1.1:1080/
proxy-url: ""

# Proxy Manager controls automatic proxy assignment, health checking, and ban-aware rotation.
# When enabled, auth entries without a proxy_id/proxy_url are automatically assigned from the
# proxy-pool, unhealthy proxies are detected and disabled, and proxies associated with banned
# accounts are taken offline to prevent further account losses.
#
# proxy-manager:
#   # Automatic proxy assignment for auth entries that have no proxy configured.
#   proxy-assignment:
#     enabled: true
#     strategy: spread              # spread | round-robin | random
#     providers:                    # only assign to these providers (empty = all)
#       - codex
#       - claude
#
#   # Background health checking for enabled proxy-pool entries.
#   proxy-health:
#     enabled: true
#     interval: 5m                  # check interval
#     timeout: 10s                  # per-check timeout
#     failure-threshold: 3          # consecutive failures before auto-disable
#     recovery-interval: 15m        # retry a disabled proxy after this long
#     test-url: "https://api.openai.com/v1/models"
#
#   # Ban-aware rotation: when accounts using the same proxy get banned, auto-disable it.
#   proxy-ban-detect:
#     enabled: true
#     ban-threshold: 3              # bans within window to trigger proxy disable
#     window-minutes: 60            # rolling window in minutes
#     auto-disable-proxy: true      # disable the proxy when threshold is reached

# When true, disables TLS certificate verification for all upstream HTTPS connections.
# Use only when upstream APIs use self-signed certificates or in trusted internal networks.
# Default is false (verification enabled).
insecure-skip-verify: false

# Path to a PEM-encoded CA certificate file for verifying upstream server certificates.
# When empty, the system default root CA pool is used.
ca-cert: ""

# When true, unprefixed model requests only use credentials without a prefix (except when prefix == model name).
force-model-prefix: false

# When true, forward filtered upstream response headers to downstream clients.
# Default is false (disabled).
passthrough-headers: false

# Number of times to retry a request. Retries will occur if the HTTP response code is 403, 408, 500, 502, 503, or 504.
request-retry: 3

# Maximum wait time in seconds for a cooled-down credential before triggering a retry.
max-retry-interval: 30

# Quota exceeded behavior
quota-exceeded:
  switch-project: true # Whether to automatically switch to another project when a quota is exceeded
  switch-preview-model: true # Whether to automatically switch to a preview model when a quota is exceeded

# Routing strategy for selecting credentials when multiple match.
routing:
  strategy: "round-robin" # round-robin (default), fill-first
  include-default-group: true
  # channel-groups:
  #   - name: "pro"
  #     description: "Paid channels"
  #     priority: 100
  #     match:
  #       prefixes: ["pro"]
  #   - name: "team-a"
  #     description: "Team A channels"
  #     match:
  #       prefixes: ["team-a"]
  #       channels: ["Team A Codex"]
  # path-routes:
  #   - path: "/pro"
  #     group: "pro"
  #     strip-prefix: true
  #     fallback: "none"
  #   - path: "/team-a"
  #     group: "team-a"
  #     strip-prefix: true
  #     fallback: "default"

# Optional client API keys with per-key permissions. Empty allowed-* arrays mean "no restriction".
# api-key-entries:
#   - key: "sk-user-pro"
#     name: "Pro User"
#     allowed-channel-groups: ["pro"]
#   - key: "sk-team-a"
#     name: "Team A"
#     allowed-channel-groups: ["team-a", "pro"]
#     allowed-channels: ["Team A Codex"]

# When true, enable authentication for the WebSocket API (/v1/ws).
ws-auth: true

# When true, enable Gemini CLI internal endpoints (/v1internal:*).
# Default is false for safety.
enable-gemini-cli-endpoint: false

# When > 0, emit blank lines every N seconds for non-streaming responses to prevent idle timeouts.
nonstream-keepalive-interval: 0

# Streaming behavior (SSE keep-alives + safe bootstrap retries).
streaming:
  keepalive-seconds: 15   # Default: 15. Set < 0 to disable keep-alives.
  bootstrap-retries: 1    # Default: 1. Retries before first byte is sent.

# Gemini API keys
# gemini-api-key:
#   - api-key: "AIzaSy...01"
#     prefix: "test" # optional: require calls like "test/gemini-3-pro-preview" to target this credential
#     base-url: "https://generativelanguage.googleapis.com"
#     headers:
#       X-Custom-Header: "custom-value"
#     proxy-url: "socks5://proxy.example.com:1080"
#     models:
#       - name: "gemini-2.5-flash" # upstream model name
#         alias: "gemini-flash"    # client alias mapped to the upstream model
#     excluded-models:
#       - "gemini-2.5-pro"     # exclude specific models from this provider (exact match)
#       - "gemini-2.5-*"       # wildcard matching prefix (e.g. gemini-2.5-flash, gemini-2.5-pro)
#       - "*-preview"          # wildcard matching suffix (e.g. gemini-3-pro-preview)
#       - "*flash*"            # wildcard matching substring (e.g. gemini-2.5-flash-lite)
#   - api-key: "AIzaSy...02"

# Codex API keys
# codex-api-key:
#   - api-key: "sk-atSM..."
#     prefix: "test" # optional: require calls like "test/gpt-5-codex" to target this credential
#     base-url: "https://www.example.com" # use the custom codex API endpoint
#     headers:
#       X-Custom-Header: "custom-value"
#     proxy-url: "socks5://proxy.example.com:1080" # optional: per-key proxy override
#     models:
#       - name: "gpt-5-codex"   # upstream model name
#         alias: "codex-latest" # client alias mapped to the upstream model
#     excluded-models:
#       - "gpt-5.1"         # exclude specific models (exact match)
#       - "gpt-5-*"         # wildcard matching prefix (e.g. gpt-5-medium, gpt-5-codex)
#       - "*-mini"          # wildcard matching suffix (e.g. gpt-5-codex-mini)
#       - "*codex*"         # wildcard matching substring (e.g. gpt-5-codex-low)

# Claude API keys
# claude-api-key:
#   - api-key: "sk-atSM..." # use the official claude API key, no need to set the base url
#   - api-key: "sk-atSM..."
#     prefix: "test" # optional: require calls like "test/claude-sonnet-latest" to target this credential
#     base-url: "https://www.example.com" # use the custom claude API endpoint
#     headers:
#       X-Custom-Header: "custom-value"
#     proxy-url: "socks5://proxy.example.com:1080" # optional: per-key proxy override
#     models:
#       - name: "claude-3-5-sonnet-20241022" # upstream model name
#         alias: "claude-sonnet-latest"      # client alias mapped to the upstream model
#     excluded-models:
#       - "claude-opus-4-5-20251101" # exclude specific models (exact match)
#       - "claude-3-*"               # wildcard matching prefix (e.g. claude-3-7-sonnet-20250219)
#       - "*-thinking"               # wildcard matching suffix (e.g. claude-opus-4-5-thinking)
#       - "*haiku*"                  # wildcard matching substring (e.g. claude-3-5-haiku-20241022)
#     cloak:                         # optional: request cloaking for non-Claude-Code clients
#       mode: "auto"                 # "auto" (default): cloak only when client is not Claude Code
#                                    # "always": always apply cloaking
#                                    # "never": never apply cloaking
#       strict-mode: false           # false (default): prepend Claude Code prompt to user system messages
#                                    # true: strip all user system messages, keep only Claude Code prompt
#       sensitive-words:             # optional: words to obfuscate with zero-width characters
#         - "API"
#         - "proxy"
#       cache-user-id: true          # optional: default is false; set true to reuse cached user_id per API key instead of generating a random one each request

# Default headers for Claude API requests. Update when Claude Code releases new versions.
# These are used as fallbacks when the client does not send its own headers.
# claude-header-defaults:
#   user-agent: "claude-cli/2.1.44 (external, sdk-cli)"
#   package-version: "0.74.0"
#   runtime-version: "v24.3.0"
#   timeout: "600"

# Zhipu Coding Plan API keys. This is intentionally separate from the generic
# OpenAI compatibility pool because Coding Plan uses provider-specific payload,
# MCP, multimodal, and Codex identity handling while exposing an OpenAI Chat
# Completions HTTP endpoint.
# bigmodel-coding:
#   - base-url: "https://open.bigmodel.cn/api/coding/paas/v4"
#     api-key-entries:
#       - api-key: "sk-..."
#     test-model: "glm-5.1"
#     identity-fingerprint: "codex"
#     models:
#       - name: "glm-5.1"
#         alias: "gpt-5.3-codex"

# OpenAI compatibility providers
# openai-compatibility:
#   - name: "openrouter" # The name of the provider; it will be used in the user agent and other places.
#     prefix: "test" # optional: require calls like "test/kimi-k2" to target this provider's credentials
#     base-url: "https://openrouter.ai/api/v1" # The base URL of the provider.
#     headers:
#       X-Custom-Header: "custom-value"
#     # optional: how /v1/images/edits is sent upstream. Use "passthrough" for
#     # native edits endpoints, "image-generations" or "chat-multimodal" for
#     # providers that do not support OpenAI-style edits directly. Empty defaults
#     # to "passthrough".
#     image-edits-mode: "passthrough"
#     # optional with image-edits-mode: "image-generations"; supported values
#     # are "image" and "image_url". Empty defaults to "image".
#     image-generations-image-field: ""
#     api-key-entries:
#       - api-key: "sk-or-v1-...b780"
#         proxy-url: "socks5://proxy.example.com:1080" # optional: per-key proxy override
#       - api-key: "sk-or-v1-...b781" # without proxy-url
#     models: # The models supported by the provider.
#       - name: "moonshotai/kimi-k2:free" # The actual model name.
<<<<<<< HEAD
#         alias: "kimi-k2" # The alias used in the API.
=======
#         alias: "kimi-k2"               # The alias used in the API.
#         image: false                   # optional: set true to allow this model on /v1/images/generations and /v1/images/edits
#         thinking:                      # optional: omit to default to levels ["low","medium","high"]
#           levels: ["low", "medium", "high"]
#       # You may repeat the same alias to build an internal model pool.
#       # The client still sees only one alias in the model list.
#       # Requests to that alias will round-robin across the upstream names below,
#       # and if the chosen upstream fails before producing output, the request will
#       # continue with the next upstream model in the same alias pool.
#       - name: "deepseek-v3.1"
#         alias: "claude-opus-4.66"
#       - name: "glm-5"
#         alias: "claude-opus-4.66"
#       - name: "kimi-k2.5"
#         alias: "claude-opus-4.66"
>>>>>>> upstream/main

# Vertex API keys (Vertex-compatible endpoints, use API key + base URL)
# vertex-api-key:
#   - api-key: "vk-123..."                        # x-goog-api-key header
#     prefix: "test"                              # optional: require calls like "test/vertex-pro" to target this credential
#     base-url: "https://example.com/api"         # e.g. https://zenmux.ai/api
#     proxy-url: "socks5://proxy.example.com:1080" # optional per-key proxy override
#     headers:
#       X-Custom-Header: "custom-value"
#     models:                                     # optional: map aliases to upstream model names
#       - name: "gemini-2.5-flash"                # upstream model name
#         alias: "vertex-flash"                   # client-visible alias
#       - name: "gemini-2.5-pro"
#         alias: "vertex-pro"

# Amp Integration
# ampcode:
#   # Configure upstream URL for Amp CLI OAuth and management features
#   upstream-url: "https://ampcode.com"
#   # Optional: Override API key for Amp upstream (otherwise uses env or file)
#   upstream-api-key: ""
#   # Per-client upstream API key mapping
#   # Maps client API keys (from top-level api-keys) to different Amp upstream API keys.
#   # Useful when different clients need to use different Amp accounts/quotas.
#   # If a client key isn't mapped, falls back to upstream-api-key (default behavior).
#   upstream-api-keys:
#     - upstream-api-key: "amp_key_for_team_a"    # Upstream key to use for these clients
#       api-keys:                                 # Client keys that use this upstream key
#         - "your-api-key-1"
#         - "your-api-key-2"
#     - upstream-api-key: "amp_key_for_team_b"
#       api-keys:
#         - "your-api-key-3"
#   # Restrict Amp management routes (/api/auth, /api/user, etc.) to localhost only (default: false)
#   restrict-management-to-localhost: false
#   # Force model mappings to run before checking local API keys (default: false)
#   force-model-mappings: false
#   # Amp Model Mappings
#   # Route unavailable Amp models to alternative models available in your local proxy.
#   # Useful when Amp CLI requests models you don't have access to (e.g., Claude Opus 4.5)
#   # but you have a similar model available (e.g., Claude Sonnet 4).
#   model-mappings:
#     - from: "claude-opus-4-5-20251101"          # Model requested by Amp CLI
#       to: "gemini-claude-opus-4-5-thinking"     # Route to this available model instead
#     - from: "claude-sonnet-4-5-20250929"
#       to: "gemini-claude-sonnet-4-5-thinking"
#     - from: "claude-haiku-4-5-20251001"
#       to: "gemini-2.5-flash"

# Global OAuth model name aliases (per channel)
# These aliases rename model IDs for both model listing and request routing.
# Supported channels: gemini-cli, vertex, aistudio, antigravity, claude, codex, qwen, iflow, kimi, xai.
# NOTE: Aliases do not apply to gemini-api-key, codex-api-key, claude-api-key, openai-compatibility, vertex-api-key, or ampcode.
# You can repeat the same name with different aliases to expose multiple client model names.
# oauth-model-alias:
#   gemini-cli:
#     - name: "gemini-2.5-pro"          # original model name under this channel
#       alias: "g2.5p"                  # client-visible alias
#       fork: true                      # when true, keep original and also add the alias as an extra model (default: false)
#   vertex:
#     - name: "gemini-2.5-pro"
#       alias: "g2.5p"
#   aistudio:
#     - name: "gemini-2.5-pro"
#       alias: "g2.5p"
#   antigravity:
#     - name: "gemini-3-pro-high"
#       alias: "gemini-3-pro-preview"
#   claude:
#     - name: "claude-sonnet-4-5-20250929"
#       alias: "cs4.5"
#   codex:
#     - name: "gpt-5"
#       alias: "g5"
#   qwen:
#     - name: "qwen3-coder-plus"
#       alias: "qwen-plus"
#   iflow:
#     - name: "glm-4.7"
#       alias: "glm-god"
#   kimi:
#     - name: "kimi-k2.5"
#       alias: "k2.5"
#   xai:
#     - name: "grok-4.3"
#       alias: "grok-latest"

# OAuth provider excluded models
# oauth-excluded-models:
#   gemini-cli:
#     - "gemini-2.5-pro"     # exclude specific models (exact match)
#     - "gemini-2.5-*"       # wildcard matching prefix (e.g. gemini-2.5-flash, gemini-2.5-pro)
#     - "*-preview"          # wildcard matching suffix (e.g. gemini-3-pro-preview)
#     - "*flash*"            # wildcard matching substring (e.g. gemini-2.5-flash-lite)
#   vertex:
#     - "gemini-3-pro-preview"
#   aistudio:
#     - "gemini-3-pro-preview"
#   antigravity:
#     - "gemini-3-pro-preview"
#   claude:
#     - "claude-3-5-haiku-20241022"
#   codex:
#     - "gpt-5-codex-mini"
#   qwen:
#     - "vision-model"
#   iflow:
#     - "tstars2.0"
#   kimi:
#     - "kimi-k2-thinking"
#   xai:
#     - "grok-3-mini"

# Optional payload configuration
# payload:
#   default: # Default rules only set parameters when they are missing in the payload.
#     - models:
#         - name: "gemini-2.5-pro" # Supports wildcards (e.g., "gemini-*")
#           protocol: "gemini" # restricts the rule to a specific protocol, options: openai, gemini, claude, codex, antigravity
#           from-protocol: "responses" # restricts the rule to the source protocol, options: openai, responses, gemini, claude
#           headers: # all configured request headers must match; values support "*" wildcards
#             X-Client-Tier: "tenant-*-region-*"
#           match: # all payload JSON paths must equal the configured values
#             - "metadata.client": "codex"
#           not-match: # payload JSON paths must not equal the configured values
#             - "metadata.mode": "dev"
#           exist: # all payload JSON paths must exist and not be null
#             - "tools.#(type==\"web_search\").type"
#           not-exist: # all payload JSON paths must be missing or null
#             - "metadata.disable_payload"
#       params: # JSON path (gjson/sjson syntax) -> value
#         "generationConfig.thinkingConfig.thinkingBudget": 32768
#   default-raw: # Default raw rules set parameters using raw JSON when missing (must be valid JSON).
#     - models:
#         - name: "gemini-2.5-pro" # Supports wildcards (e.g., "gemini-*")
#           protocol: "gemini" # restricts the rule to a specific protocol, options: openai, gemini, claude, codex, antigravity
#       params: # JSON path (gjson/sjson syntax) -> raw JSON value (strings are used as-is, must be valid JSON)
#         "generationConfig.responseJsonSchema": "{\"type\":\"object\",\"properties\":{\"answer\":{\"type\":\"string\"}}}"
#   override: # Override rules always set parameters, overwriting any existing values.
#     - models:
#         - name: "gpt-*" # Supports wildcards (e.g., "gpt-*")
#           protocol: "codex" # restricts the rule to a specific protocol, options: openai, gemini, claude, codex, antigravity
#       params: # JSON path (gjson/sjson syntax) -> value
#         "reasoning.effort": "high"
#   override-raw: # Override raw rules always set parameters using raw JSON (must be valid JSON).
#     - models:
#         - name: "gpt-*" # Supports wildcards (e.g., "gpt-*")
#           protocol: "codex" # restricts the rule to a specific protocol, options: openai, gemini, claude, codex, antigravity
#       params: # JSON path (gjson/sjson syntax) -> raw JSON value (strings are used as-is, must be valid JSON)
#         "response_format": "{\"type\":\"json_schema\",\"json_schema\":{\"name\":\"answer\",\"schema\":{\"type\":\"object\"}}}"
#   filter: # Filter rules remove specified parameters from the payload.
#     - models:
#         - name: "gemini-2.5-pro" # Supports wildcards (e.g., "gemini-*")
#           protocol: "gemini" # restricts the rule to a specific protocol, options: openai, gemini, claude, codex, antigravity
#       params: # JSON paths (gjson/sjson syntax) to remove from the payload
#         - "generationConfig.thinkingConfig.thinkingBudget"
#         - "generationConfig.responseJsonSchema"
